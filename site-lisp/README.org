This is a place for,

- 3rd party code which isn't available in MELPA or other package repositories
- package which is preferred to be loaded locally instead of online installation

This directory is added to load-path at start-up time.
